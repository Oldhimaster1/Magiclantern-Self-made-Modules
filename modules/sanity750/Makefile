# Module name must be at most 8 characters.
MODULE_NAME = san750

# Keep the source filename descriptive while using a short module filename.
MODULE_OBJS = $(BUILD_DIR)/sanity750.o
TOP_DIR = ../..

# Include the Magic Lantern modules build environment.
include $(TOP_DIR)/modules/Makefile

.DEFAULT_GOAL := $(BUILD_DIR)/module_complete
